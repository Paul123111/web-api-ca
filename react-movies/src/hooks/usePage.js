// import { useEffect, useState } from "react";
// import {getMovie} from '../api/tmdb-api'

// const usePage = page => {
//   const [page, setPageNumber] = useState(null);
//   useEffect(() => {
//     getPage(id).then(movie => {
//       setMovie(movie);
//     });
//   }, [id]);
//   return [movie, setMovie];
// };

// export default useMovie;