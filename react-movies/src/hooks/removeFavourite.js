// import { useEffect, useState } from "react";
// import {getMovie} from '../api/tmdb-api'

// const removeFavourite = (movies, movie) => {
//   const [array, setArray] = useState(movies);
//   useEffect(() => {
//     array.filter(movie((i) => i != movie))
//   });
//   return array;
// };

// export default removeFavourite;